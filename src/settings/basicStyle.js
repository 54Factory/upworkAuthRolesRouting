const rowStyle = {
  width: '100%',
  display: 'flex',
  flexFlow: 'row wrap',
};

const colStyle = {
  marginBottom: '16px',
};

const gutter = 16;

const appBar = {
  height: 56
};

const basicStyle = {
  rowStyle,
  colStyle,
  gutter,
  appBar
};

export default basicStyle;
