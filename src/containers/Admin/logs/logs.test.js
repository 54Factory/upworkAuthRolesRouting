/**
 * Should call getData on component mount
 * Should call getData on filter/sorting change
 * Should call getData on dateChange
 *
 * getData should construct correct collection query depending on state
 *
 * Should match snapshot
 */
